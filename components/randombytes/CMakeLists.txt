idf_component_register(SRCS "randombytes.c"
                    INCLUDE_DIRS "."
                    REQUIRES "") # Pode ser implementado standalone, ou substituir por rand do ESP-IDF se apropriado. Por enquanto standalone.
